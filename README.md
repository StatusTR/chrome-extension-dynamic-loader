# Dynamic Page Modifier v2.0

Розширення Chrome для динамічної модифікації веб-сторінок з завантаженням коду з GitHub.

## 🎯 Основні можливості

- ✅ **Popup UI** - зручний інтерфейс для управління модифікаціями
- ✅ **Головний перемикач** - швидко увімкнути/вимкнути всі модифікації
- ✅ **Індивідуальне управління** - вмикати/вимикати кожну модифікацію окремо
- ✅ **Оновлення з GitHub** - автоматичне та примусове оновлення
- ✅ **Кешування** - швидка робота з локальним кешем

## 📁 Структура файлів

```
chrome_extension_project/
├── manifest.json        # Конфігурація розширення
├── background.js        # Service Worker
├── content-script.js    # Завантажувач модифікацій
├── popup.html           # UI попапу
├── popup.js             # Логіка попапу
├── modifications.js     # Код модифікацій (GitHub)
├── icons/               # Іконки розширення
│   ├── icon16.png
│   ├── icon32.png
│   ├── icon48.png
│   └── icon128.png
├── README.md            # Ця документація
└── GUIDE.md             # Гайд для користувача
```

## 🚀 Встановлення

1. Відкрийте Chrome та перейдіть: `chrome://extensions/`
2. Увімкніть **"Режим розробника"** (Developer mode)
3. Натисніть **"Завантажити розпаковане"** (Load unpacked)
4. Виберіть папку `chrome_extension_project`

## 🎮 Як користуватися UI

### Головний перемикач
- **ON** - всі модифікації активні
- **OFF** - всі модифікації вимкнені

### Список модифікацій
- Кожна модифікація має свій перемикач
- Вимкніть непотрібні модифікації

### Кнопки
- **"Оновити з GitHub"** - примусове завантаження нового коду
- **"Перезавантажити"** - перезавантажити поточну сторінку

## 📝 Структура modifications.js

### Формат модифікацій

```javascript
const MODIFICATIONS = [
  {
    id: 'unique-id',           // Унікальний ідентифікатор
    name: 'Назва',             // Назва для UI
    description: 'Опис',      // Опис для UI
    type: 'number',            // Тип модифікації
    config: { ... }            // Конфігурація
  }
]; // END_MODIFICATIONS  <- ВАЖЛИВО: маркер кінця
```

### Типи модифікацій

#### 1. `number` - Зміна чисел/балансів
```javascript
{
  id: 'balance-mod',
  name: 'Зміна балансу',
  description: 'Додає суму до балансу',
  type: 'number',
  config: {
    increment: 1000.50,                    // Сума додавання
    selector: '.balance span'              // CSS селектор
  }
}
```

#### 2. `text` - Зміна тексту
```javascript
{
  id: 'title-mod',
  name: 'Зміна заголовка',
  description: 'Змінює текст',
  type: 'text',
  config: {
    selector: 'h1',                        // CSS селектор
    find: 'Старий текст',               // Що шукати
    replace: 'Новий текст'              // На що замінити
  }
}
```

#### 3. `hide` - Приховування елементів
```javascript
{
  id: 'hide-ads',
  name: 'Приховати рекламу',
  description: 'Приховує елементи',
  type: 'hide',
  config: {
    selectors: ['.ad', '.promo', '#banner']  // Масив селекторів
  }
}
```

#### 4. `style` - Додавання CSS
```javascript
{
  id: 'custom-css',
  name: 'Кастомні стилі',
  description: 'Додає CSS',
  type: 'style',
  config: {
    css: `
      .element { color: red !important; }
      #header { display: none; }
    `
  }
}
```

#### 5. `element` - Додавання елементів
```javascript
{
  id: 'add-badge',
  name: 'Додати бейдж',
  description: 'Додає HTML елемент',
  type: 'element',
  config: {
    targetSelector: '.header',             // Куди додати
    position: 'beforeend',                 // Позиція: beforeend, afterbegin, etc.
    html: '<div class="badge">✓</div>'    // HTML код
  }
}
```

## 🔄 Оновлення модифікацій

1. Відредагуйте `modifications.js` на GitHub
2. В попапі натисніть "Оновити з GitHub"
3. Або зачекайте 5 хвилин (автоматичне оновлення)

## 🔧 Дебагінг

1. Відкрийте DevTools (F12)
2. В консолі шукайте повідомлення `[DynamicLoader]` та `[Modifications]`
3. Для background.js: `chrome://extensions/` → "Деталі" → "Service Worker"

## 📦 GitHub Репозиторій

- **URL**: https://github.com/StatusTR/chrome-extension-dynamic-loader
- **modifications.js**: https://raw.githubusercontent.com/StatusTR/chrome-extension-dynamic-loader/main/modifications.js

## ⚠️ Безпека

- Не зберігайте чутливі дані в коді
- Публічні репозиторії видимі всім
- Код виконується на цільових сторінках
