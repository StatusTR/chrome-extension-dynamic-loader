# 📋 Інструкція з використання Chrome Extension

## 🔗 Важливі посилання

- **Репозиторій:** https://github.com/StatusTR/chrome-extension-dynamic-loader
- **Raw modifications.js:** https://raw.githubusercontent.com/StatusTR/chrome-extension-dynamic-loader/main/modifications.js

---

## 📥 Встановлення розширення в Chrome

### Крок 1: Завантаження файлів
Завантажте папку проекту на свій комп'ютер або склонуйте репозиторій:
```bash
git clone https://github.com/StatusTR/chrome-extension-dynamic-loader.git
```

### Крок 2: Відкрийте сторінку розширень Chrome
1. Відкрийте Chrome
2. В адресному рядку введіть: `chrome://extensions/`
3. Натисніть Enter

### Крок 3: Увімкніть режим розробника
1. У верхньому правому куті знайдіть перемикач **"Режим розробника"** (Developer mode)
2. Увімкніть його

### Крок 4: Завантажте розширення
1. Натисніть кнопку **"Завантажити розпакований"** (Load unpacked)
2. Виберіть папку `chrome-extension-dynamic-loader` (де знаходяться файли manifest.json, content-script.js, тощо)
3. Розширення має з'явитися в списку встановлених

### Крок 5: Перевірка
1. Відкрийте сайт https://banking.postbank.de/
2. Відкрийте DevTools (F12) → Console
3. Ви повинні бачити логи `[DynamicLoader]`

---

## 🔄 Оновлення коду модифікацій на GitHub

### Варіант 1: Через GitHub Web Interface
1. Перейдіть на https://github.com/StatusTR/chrome-extension-dynamic-loader
2. Відкрийте файл `modifications.js`
3. Натисніть іконку олівця (Edit this file)
4. Внесіть зміни
5. Внизу сторінки натисніть **"Commit changes"**

### Варіант 2: Через Git (командний рядок)
```bash
# Клонування (якщо ще не склоновано)
git clone https://github.com/StatusTR/chrome-extension-dynamic-loader.git
cd chrome-extension-dynamic-loader

# Редагування файлу
nano modifications.js  # або будь-який текстовий редактор

# Коміт та пуш
git add modifications.js
git commit -m "Update modifications"
git push origin main
```

### ⏱️ Після оновлення
- Зміни автоматично підтягнуться розширенням протягом 5 хвилин (налаштовано в CONFIG.CACHE_DURATION_MS)
- Для негайного оновлення: очистіть localStorage сайту та оновіть сторінку

---

## ➕ Додавання нових сайтів для модифікації

### Крок 1: Оновіть manifest.json
Додайте новий сайт до `host_permissions`:

```json
"host_permissions": [
  "https://banking.postbank.de/*",
  "https://newsite.example.com/*",
  "https://raw.githubusercontent.com/*"
]
```

### Крок 2: Додайте content_scripts для нового сайту
```json
"content_scripts": [
  {
    "matches": ["https://banking.postbank.de/*"],
    "js": ["content-script.js"],
    "run_at": "document_start"
  },
  {
    "matches": ["https://newsite.example.com/*"],
    "js": ["content-script.js"],
    "run_at": "document_start"
  }
]
```

### Крок 3: Оновіть modifications.js
Додайте логіку для визначення поточного сайту:

```javascript
(() => {
  'use strict';
  
  const hostname = location.hostname;
  
  if (hostname.includes('postbank.de')) {
    // Логіка для Postbank
    modifyPostbank();
  } else if (hostname.includes('newsite.example.com')) {
    // Логіка для нового сайту
    modifyNewSite();
  }
  
  function modifyPostbank() {
    // Існуючий код для Postbank
  }
  
  function modifyNewSite() {
    // Новий код для нового сайту
  }
})();
```

### Крок 4: Оновіть розширення в Chrome
1. Перейдіть до `chrome://extensions/`
2. Знайдіть ваше розширення
3. Натисніть іконку оновлення 🔄

---

## 🐛 Вирішення проблем

### Розширення не завантажується
- Перевірте, чи увімкнено "Режим розробника"
- Перевірте синтаксис JSON в manifest.json

### Модифікації не працюють
1. Відкрийте DevTools (F12) → Console
2. Перевірте наявність логів `[DynamicLoader]`
3. Перевірте наявність помилок CORS або 404

### Зміни не підтягуються з GitHub
- Очистіть localStorage: DevTools → Application → Storage → Clear site data
- Перевірте URL в content-script.js

### Встановлення DEBUG режиму
В `content-script.js` знайдіть `DEBUG: true` - це увімкне детальні логи

---

## 📁 Структура файлів

```
chrome-extension-dynamic-loader/
├── manifest.json          # Конфігурація Chrome Extension
├── content-script.js      # Завантажувач (не потребує частих змін)
├── modifications.js       # 👈 Основний код модифікацій (оновлюйте тут!)
├── README.md              # Документація проекту
└── INSTRUCTIONS.md        # Ця інструкція
```

---

## ⚠️ Важливі примітки

1. **Безпека:** Завжди перевіряйте код перед виконанням, особливо на банківських сайтах
2. **Приватність:** Репозиторій публічний - не додавайте чутливі дані
3. **Оновлення:** Після зміни manifest.json обов'язково оновіть розширення в Chrome
4. **Кешування:** За замовчуванням код кешується на 5 хвилин

---

*Створено: Лютий 2026*
