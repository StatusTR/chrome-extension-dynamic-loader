# 📖 Гайд для користувача

Цей гайд пояснює як давати фрагменти коду сторінки та описувати що потрібно змінити.

## 🔍 Як знайти потрібний елемент

### Крок 1: Відкрийте DevTools
1. Натисніть **F12** або **Права кнопка → "Інспектувати"**
2. Перейдіть на вкладку **Elements**

### Крок 2: Виберіть елемент
1. Натисніть іконку вибору елемента (стрілка в квадраті) або **Ctrl+Shift+C**
2. Наведіть на потрібний елемент і клікніть

### Крок 3: Скопіюйте HTML
1. Права кнопка на елементі в DevTools
2. **Copy → Copy outerHTML**

---

## 📝 Як описувати зміни

### Шаблон запиту

```
Тип зміни: [зміна тексту / зміна числа / приховати / додати елемент / зміна стилю]

HTML фрагмент:
[HTML код елемента]

Що зробити:
[Опис бажаної зміни]
```

---

## 💡 Приклади запитів та відповідних модифікацій

---

### 💰 Приклад 1: Зміна числа/балансу

**Запит:**
```
Тип зміни: зміна числа

HTML фрагмент:
<db-banking-decorated-amount>
  <span class="balance">
    <span>2.620,76</span>
    <span data-test="currencyCode">EUR</span>
  </span>
</db-banking-decorated-amount>

Що зробити:
Додати 100000 до всіх балансів
```

**Результат - модифікація:**
```javascript
{
  id: 'balance-modifier',
  name: 'Зміна балансу',
  description: 'Додає 100000 до балансу',
  type: 'number',
  config: {
    increment: 100000,
    selector: 'db-banking-decorated-amount .balance span:not([data-test="currencyCode"])'
  }
}
```

---

### ✏️ Приклад 2: Зміна тексту

**Запит:**
```
Тип зміни: зміна тексту

HTML фрагмент:
<h1 class="page-title">Financial Overview</h1>

Що зробити:
Змінити заголовок на "My Wealth Dashboard"
```

**Результат - модифікація:**
```javascript
{
  id: 'title-change',
  name: 'Зміна заголовка',
  description: 'Змінює заголовок сторінки',
  type: 'text',
  config: {
    selector: 'h1.page-title',
    find: 'Financial Overview',
    replace: 'My Wealth Dashboard'
  }
}
```

---

### 🚫 Приклад 3: Приховування елементів

**Запит:**
```
Тип зміни: приховати

HTML фрагмент:
<div class="ad-banner promo-section">
  <img src="promo.jpg">
  <p>Спеціальна пропозиція!</p>
</div>

Що зробити:
Приховати цей рекламний банер
```

**Результат - модифікація:**
```javascript
{
  id: 'hide-promo',
  name: 'Приховати промо',
  description: 'Приховує рекламні банери',
  type: 'hide',
  config: {
    selectors: ['.ad-banner', '.promo-section']
  }
}
```

---

### ➕ Приклад 4: Додавання елемента

**Запит:**
```
Тип зміни: додати елемент

HTML фрагмент:
<header class="main-header">
  <nav class="navigation">...</nav>
</header>

Що зробити:
Додати зелений бейдж "VIP" в хедер
```

**Результат - модифікація:**
```javascript
{
  id: 'vip-badge',
  name: 'VIP бейдж',
  description: 'Додає VIP бейдж в хедер',
  type: 'element',
  config: {
    targetSelector: 'header.main-header',
    position: 'beforeend',
    html: '<div style="background:#22c55e;color:white;padding:6px 12px;border-radius:20px;font-weight:bold;font-size:14px;position:absolute;right:20px;top:50%;transform:translateY(-50%);">VIP</div>'
  }
}
```

---

### 🎨 Приклад 5: Зміна стилів

**Запит:**
```
Тип зміни: зміна стилю

HTML фрагмент:
<div class="account-card">
  <span class="balance">1.234,56 EUR</span>
</div>

Що зробити:
Зробити баланс більшим і зеленим
```

**Результат - модифікація:**
```javascript
{
  id: 'balance-style',
  name: 'Стиль балансу',
  description: 'Збільшує та забарвлює баланс',
  type: 'style',
  config: {
    css: `
      .account-card .balance {
        font-size: 28px !important;
        font-weight: bold !important;
        color: #22c55e !important;
      }
    `
  }
}
```

---

## 🛠️ Поради для CSS селекторів

### Основні селектори:

| Селектор | Опис | Приклад |
|-----------|------|--------|
| `.class` | По класу | `.balance`, `.ad-banner` |
| `#id` | По ID | `#header`, `#main-content` |
| `tag` | По тегу | `h1`, `div`, `span` |
| `tag.class` | Тег з класом | `span.amount`, `div.card` |
| `.parent .child` | Вкладений | `.card .balance` |
| `[attr]` | По атрибуту | `[data-test]` |
| `[attr="val"]` | Атрибут = значення | `[data-test="amount"]` |
| `:not(sel)` | Виключити | `:not(.hidden)` |

### Як отримати селектор:

1. Виберіть елемент в DevTools
2. Права кнопка → **Copy → Copy selector**
3. Обріжте зайві частини

---

## ⚠️ Важливі примітки

1. **Унікальні ID** - кожна модифікація повинна мати унікальний `id`
2. **Маркер кінця** - масив MODIFICATIONS повинен закінчуватися `]; // END_MODIFICATIONS`
3. **Тестування** - перевірте селектор в консолі: `document.querySelectorAll('your-selector')`

---

## 📞 Підтримка

Якщо щось не працює:
1. Відкрийте DevTools (F12) → Console
2. Шукайте помилки `[Modifications]` або `[DynamicLoader]`
3. Перевірте селектор: `document.querySelector('your-selector')`
